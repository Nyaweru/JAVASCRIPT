// block of code that performs a task and is executed when invoked
/* syntax : Function keyword then name , parameters that are in brackets () ie function Product(10.9);
code to be execruted is inside the curly brackets {}
arguments are the values received when function is invoked
function is invoked if an event occurs ie, click event, automatically invoked, invoked in javascript code
the operator () calls the function
*/

function getDifference(){
    let x = 5;
    let y = 3;
    return x-y ;
 
}
console.log("difference:", getDifference());

  
  

  // functions as variable values
  
  //local variables are only the ones inside a function.
